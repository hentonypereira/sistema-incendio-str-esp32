[wokwi]
version = 1

firmware = ".pio/build/esp32dev/firmware.bin"
elf = ".pio/build/esp32dev/firmware.elf"

[[net.forward]]
from = "localhost:8080"
to = "target:8080"

#target: dispositivo simulado, nesse caso é o ESP32